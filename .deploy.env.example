# Copy to .deploy.env (never commit). Used by update-server.bat / deploy/remote_update.py

PMAS_SSH_HOST=server.linooxel.com
PMAS_SSH_PORT=185
PMAS_SSH_USER=root
PMAS_SSH_PASS=

# Repo path on the server
PMAS_REMOTE_DIR=/root/termeh/PMASS
